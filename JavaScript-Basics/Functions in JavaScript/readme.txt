Here are 69 function-related coding exercises for beginners to practice and better understand the concept of functions in JavaScript:

1.Write a function that takes two numbers as parameters and returns their sum.

2.Write a function that takes a string as a parameter and returns the length of the string.

3.Write a function that takes an array of numbers as a parameter and returns the sum of all the numbers in the array.

4.Write a function that takes a string as a parameter and returns the string in reverse.

5.Write a function that takes an array of strings as a parameter and returns the concatenated string of all elements in the array.

6.Write a function that takes an array of numbers as a parameter and returns the highest number in the array.

7.Write a function that takes an array of numbers as a parameter and returns a new array with all the numbers doubled.

8.Write a function that takes an array of strings as a parameter and returns a new array with the length of each string.

9.Write a function that takes two strings as parameters and returns true if the two strings are anagrams (contain the same letters but in a different order), otherwise false.

10.Write a function that takes a number as a parameter and returns true if it is even, otherwise false.

11.Write a function that takes an array of numbers as a parameter and returns a new array with only the even numbers.

12.Write a function that takes a sentence as a parameter and returns the number of words in the sentence.

13.Write a function that takes an array of numbers as a parameter and returns the product of all the numbers in the array.

14.Write a function that takes a string as a parameter and returns the number of vowels in the string.

15.Write a function that takes a number as a parameter and returns true if it is a prime number, otherwise false.

16.Write a function that takes an array of numbers as a parameter and returns a new array with the numbers sorted in ascending order.

17.Write a function that takes two arrays as parameters and returns a new array with the elements of both arrays combined.

18.Write a function that takes a string as a parameter and returns true if it is a palindrome (reads the same backward as forward), otherwise false.

19.Write a function that takes an array of strings as a parameter and returns a new array with the strings sorted in alphabetical order.

20.Write a function that takes a sentence as a parameter and returns the sentence with all the words capitalized.
Write a function that takes a string as a parameter and returns the number of uppercase letters in the string.

21.Write a function that takes an array of numbers as a parameter and returns the sum of all the positive numbers in the array.

22.Write a function that takes an array of strings as a parameter and returns a new array with the strings sorted in alphabetical order.

23.Write a function that takes a number as a parameter and returns the factorial of that number.

24.Write a function that takes an array of objects representing students (with properties like name, age, and grade) and returns an array of the names of students who have a grade higher than 80.

25.Write a function that takes a sentence as a parameter and returns the longest word in the sentence.

26.Write a function that takes an array of numbers as a parameter and returns a new array with the numbers squared.

27.Write a function that takes an object representing a person (with properties like name, age, and gender) and returns a greeting message like "Hello, John! You are 25 years old."

28.Write a function that takes a string as a parameter and returns true if it is a valid email address (has the format "user@example.com"), otherwise false.28

29.Write a function that takes an array of strings as a parameter and returns a new array with the strings reversed.

30.Write a function that takes an array of numbers as a parameter and returns the average of all the numbers.

31.Write a function that takes an array of objects representing books (with properties like title, author, and pages) and returns the total number of pages of all the books.

32.Write a function that takes an object representing a car (with properties like make, model, and year) and returns a sentence describing the car like "This is a 2020 Honda Civic."

33.Write a function that takes a number as a parameter and returns the sum of all the numbers from 1 to that number.

34.Write a function that takes a sentence as a parameter and returns the number of occurrences of each word in the sentence as an object.

35.Write a function that takes an array of numbers as a parameter and returns the largest number in the array.

36.Write a function that takes an array of strings as a parameter and returns a new array with only the strings that have more than 5 characters.

37.Write a function that takes a number as a parameter and returns true if it is a perfect square (i.e., the square root is a whole number), otherwise false.

38.Write a function that takes an array of objects representing products (with properties like name, price, and quantity) and returns the total cost of all the products.

39.Write a function that takes a string as a parameter and returns true if it is a palindrome (reads the same backward as forward), otherwise false.

40.Write a function that takes an array of numbers as a parameter and returns a new array with only the prime numbers.

41.Write a function that takes an array of strings as a parameter and returns the number of words in the array.

42.Write a function that takes an array of objects representing students (with properties like name, age, and grade) and returns the average grade of all the students.

43.Write a function that takes a number as a parameter and returns true if it is an Armstrong number (an n-digit number that is equal to the sum of its own digits each raised to the power of n), otherwise false.

44.Write a function that takes an array of numbers as a parameter and returns a new array with the numbers in descending order.

45.Write a function that takes an object representing a book (with properties like title, author, and pages) and returns a sentence describing the book like "The Lord of the Rings by J. R. R. Tolkien is a fantasy book with 1216 pages."

46.Write a function that takes a sentence as a parameter and returns the number of vowels in the sentence.

47.Write a function that takes an array of strings as a parameter and returns a new array with the strings in reverse order.

48.Write a function that takes a number as a parameter and returns the Fibonacci sequence up to that number.

49.Write a function that takes an array of objects representing students (with properties like name, age, and grade) and returns the names of the students sorted by grade in ascending order.

50.Write a function that takes a sentence as a parameter and returns the number of words that start with a vowel.

51.Write a function that takes an array of numbers as a parameter and returns the median of the numbers.

52.Write a function that takes an array of strings as a parameter and returns a new array with only the strings that contain the letter "a".

53.Write a function that takes a number as a parameter and returns true if it is a prime number, otherwise false.

54.Write a function that takes an array of numbers as a parameter and returns a new array with the numbers rounded to the nearest integer.

55.Write a function that takes an object representing a student (with properties like name, age, and grade) and returns true if the student has passed (grade is greater than or equal to 50), otherwise false.

56.Write a function that takes a sentence as a parameter and returns the number of occurrences of each letter in the sentence as an object.

57.Write a function that takes an array of objects representing books (with properties like title, author, and pages) and returns an array of the titles of books with more than 500 pages.

58.Write a function that takes a number as a parameter and returns the sum of all the multiples of 3 and 5 up to that number.

59.Write a function that takes an array of strings as a parameter and returns a new array with the strings sorted by length in ascending order.

60.Write a function that takes an array of numbers as a parameter and returns the number of odd numbers in the array.

61.Write a function that takes an object representing a car (with properties like make, model, and year) and returns true if it is an electric car (has the property "electric" set to true), otherwise false.

62.Write a function that takes a sentence as a parameter and returns the number of occurrences of each word that starts with a capital letter.

63.Write a function that takes an array of strings as a parameter and returns a new array with only the strings that are palindromes.

64.Write a function that takes a number as a parameter and returns true if it is a perfect number (the sum of its divisors is equal to the number itself), otherwise false.

65.Write a function that takes an array of numbers as a parameter and returns a new array with the numbers that are multiples of 2 and 3.

66.Write a function that takes an object representing a student (with properties like name, age, and grade) and returns true if the student is a teenager (age is between 13 and 19), otherwise false.

67.Write a function that takes a sentence as a parameter and returns the number of occurrences of each word that ends with the letter "s".

68.Write a function that takes an array of numbers as a parameter and returns a new array with only the numbers that are divisible by 4.

69.Write a function that takes an object representing a car (with properties like make, model, and year) and returns true if it is a vintage car (year is between 20 and 50 years old), otherwise false.


These exercises cover a variety of scenarios and will help you reinforce your understanding of functions in JavaScript
